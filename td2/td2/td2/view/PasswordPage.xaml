<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="td2.view.PasswordPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Enregistrer" Clicked="Save">

        </ToolbarItem>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="Mot de passe actuel" />
            <Entry IsPassword="True" Text="{Binding Mdp.OldPassword}" Placeholder="Entrez votre mot de passe actuel" />
            <Label Text="Nouveau mot de passe" />
            <Entry IsPassword="True" Text="{Binding Mdp.NewPassword}" Placeholder="Entrez votre nouveau mot de passe" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>